pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'source ~/.cargo/env && cargo build'
            }
        }

        stage('Test') {
            steps {
                sh 'source ~/.cargo/env && cargo test'
            }
        }
    }
    post {
        always {
            cleanWs()
        }
    }
}